
/*
std::string reverse_string(std::string input) {
    Stack<char> stack;
    std::string reversed{};
    
    for (const char c: input) {
        stack.push(c);
    }
    
    while (!stack.is_empty()) {
        reversed += stack.pop();
    }
    
    return reversed;
}
*/